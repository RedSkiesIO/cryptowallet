const actions = {
  setAccountLocale: jest.fn(),
  setAccountType: jest.fn(),
};

const getters = {};
const mutations = {};
const state = {};

const account = {
  namespaced: true,
  actions,
  getters,
  mutations,
  state,
};

export default account;
