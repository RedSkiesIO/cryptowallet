build-develop:
  stage: build
  script:
    - docker build . --tag acmwunittest:latest
    - docker run -v ${PWD}:/srv acmwunittest:latest sh -c "cd srv && npm install && npm run test:unit"
  only:
    - develop
