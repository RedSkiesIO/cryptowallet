/**
 * State mutation for salt
 * @param {*} state
 */
export function SET_SALT(state, salt) {
  state.salt = salt;
}

/**
 * State mutation for pin
 * @param {*} state
 */
export function SET_PIN(state, pin) {
  state.pin = pin;
}

