// app global css

@font-face
    font-family Montserrat-Thin
    src url(./fonts/Montserrat/Montserrat-Thin.ttf)

@font-face
    font-family Montserrat-Light
    src url(./fonts/Montserrat/Montserrat-Light.ttf)

@font-face
    font-family Montserrat-Regular
    src url(./fonts/Montserrat/Montserrat-Regular.ttf)

@font-face
    font-family Montserrat-Medium
    src url(./fonts/Montserrat/Montserrat-Medium.ttf)

@font-face
    font-family Montserrat-Semibold
    src url(./fonts/Montserrat/Montserrat-SemiBold.ttf)

@font-face
    font-family Montserrat-Bold
    src url(./fonts/Montserrat/Montserrat-Bold.ttf)

@font-face
    font-family CooperHewitt-Semibold
    src url(./fonts/CooperHewitt/CooperHewitt-Semibold.otf)

@font-face
    font-family CooperHewitt-SemiboldItalic
    src url(./fonts/CooperHewitt/CooperHewitt-SemiboldItalic.otf)

@font-face
    font-family CooperHewitt-BoldItalic
    src url(./fonts/CooperHewitt/CooperHewitt-BoldItalic.otf)

@font-face
    font-family CooperHewitt-Bold
    src url(./fonts/CooperHewitt/CooperHewitt-Bold.otf)

@font-face
    font-family CooperHewitt-Semibold
    src url(./fonts/CooperHewitt/CooperHewitt-Semibold.otf)

@font-face
    font-family CooperHewitt-HeavyItalic
    src url(./fonts/CooperHewitt/CooperHewitt-HeavyItalic.otf)

body #q-app
    font-family Montserrat-Regular
    background #e4e9ef;

.modal-backdrop {
    background: rgba(0, 0, 0, 0.5);
}

.modal-content {
    color: #4a4a4a;
    box-shadow: none;
}

button.q-btn {
    box-shadow: none;
    font-family: Montserrat-Bold;
}

button.q-btn.disabled,
button.q-btn[disabled] {
    opacity: 0.3!important;
}

.text-blueish {
  color: #1e3c57;
}

.q-input.bg-blueish {
  background: #33597b;
}

.bg-blueish {
  background: #1e3c57;
}

.text-cyan {
  color: #78d2e6;
}

.bg-cyan {
  background: #78d2e6;
}

.text-yellow {
  color: #fabc57!important;
}

.bg-yellow {
  background: #fabc57!important;
}

.text-orange {
    color: #fabc57;
}

.bg-orange {
    background: #fabc57;
}

.modal-header {
  font-family: Montserrat-Medium;
  color: #252525;
}

.q-notifications > div.column {
  display: flex
}

.wizard-header, .wizard-icon-circle, .wizard-nav, .wizard-progress-with-circle{
  display: none;
    visibility: hidden;
}

.layout-wrapper {
  display: flex;
  flex-direction: column;
  height: 100vh;
  padding-top: 2.5rem;
  padding-bottom: 5rem;
  position: relative;
  color: white;
}

.q-pull-to-refresh-wrapper .layout-wrapper {
  padding-top: 0;
  height: calc(100vh - 2.5rem);
}

.layout-wrapper > div,
.setup-wrapper {
    height: 100%;
    display: flex;
    flex-direction: column;
    z-index: 2;
}

.layout-wrapper > div > section:last-of-type {
  flex-grow: 1;
}

.layout-wrapper.no-footer {
    height: 100vh;
    padding-bottom: 0;
}

.q-header,
.q-footer,
.q-btn-group,
{
    box-shadow: none;
}

.setup-wrapper,
.center-content-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 100%;
  padding: 0 0.5em;
}

.container {
    width: 100%;
}

.q-btn.disabled {
    opacity: 0.2 !important;
}

.wizard-nav.wizard-nav-pills,
.wizard-header,
.wizard-progress-with-circle {
  display: none;
}

h1.setup {
  text-align: center;
  font-family: CooperHewitt-Semibold;
  margin-top: 0;
  font-size: 1.8rem;
}

h4.setup {
  text-align: center;
  margin: 0.5em auto;
  font-size: 1rem;
}

p.setup {
  text-align: center;
  margin: 0.5em auto;
  font-size: 0.9rem;
  line-height: 1.5;
}

.btns-wrapper {
  display: flex;
  justify-content: center;
  margin-top: 1rem;
  justify-content: space-around;
}

.layout-shape {
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}

.layout-shape {
  height: 100%;
  margin: 0 .5em;
  border-top-right-radius: 0.5em;
  border-top-left-radius: 0.5em;
  color: #252525;
}

.layout-shape > div {
  width: 100%;
  height: 100%;
}

.shrinked .layout-shape {
  overflow: hidden;
  /* padding: .25rem .25rem .25rem .25rem */
}

.shrinked .layout-shape .layout {
}

/*! Typebase.less v0.1.0 | MIT License */

// Typesetting variables. Edit these!
baseFontSize =    22 // in pixels. This would result in 22px on desktop
baseLineHeight = 1.5 // how large the line height is as a multiple of font size
leading =               baseLineHeight * 1rem
// Rate of growth for headings
// I actually like this to be slightly smaller then the leading. Makes for tight headings.
scale = 1.414

/* Setup */

html
    /* Change default typefaces here */
    font-family serif
    font-size 16px
    // Make everything look a little nicer in webkit
    -webkit-font-smoothing antialiased
    // -webkit-text-size-adjust auto
    // -moz-text-size-adjust auto
    // -ms-text-size-adjust auto
    // -o-text-size-adjust auto
    // text-size-adjust auto

/* Copy & Lists */
p
    line-height        leading
    margin-top       leading
    margin-bottom 0

ul,
ol
    margin-top leading
    margin-bottom leading
    li
        line-height leading

    ul,
    ol
        margin-top 0
        margin-bottom 0


blockquote
    line-height leading
    margin-top leading
    margin-bottom leading

/* Headings */

h1,
h2,
h3,
h4,
h5,
h6
    /* Change heading typefaces here */
    margin-top       leading
    margin-bottom 0
    line-height        leading

h1
    font-size      1.5 * scale * 1rem
    line-height   1.5 * leading
    margin-top  1.5 * leading

h2
    font-size     1.25 * scale * 1rem
    line-height  1.25 * leading
    margin-top 1.25 * leading

h3
    font-size     1 * scale * 1rem

h4
    font-size     scale / 2 * 1rem

h5
    font-size     scale / 3 * 1rem

h6
    font-size     scale / 4 * 1rem

/* Tables */

table
    margin-top         leading
    border-spacing  0px
    border-collapse collapse

td,
th
    padding     0
    line-height baseLineHeight * baseFontSize - 0px

/* Code blocks */

code
    // Forces text to constrain to the line-height. Not ideal, but works.
    vertical-align bottom

/* Leading paragraph text */

.lead
    font-size scale * 1rem

.q-btn:not(.disabled):not(.q-btn-flat):not(.q-btn-outline):not(.q-btn-push):active:before, .q-btn:not(.disabled):not(.q-btn-flat):not(.q-btn-outline):not(.q-btn-push).active:before {
    box-shadow: none;
}

.q-alert {
  box-shadow: none;
}

.q-option .q-option-label{
  opacity: 0.6;
}

.q-option[disabled] .q-option-label{
  opacity: 1;
}

.q-option[disabled] {
  opacity: 1!important;
}

.q-alert.bg-info {
  color: #1e3c57!important;
}

button.bg-secondary {
  background: rgba(255, 255, 255, 0.05)!important;
}

/* background: #fabc57!important;
    color: #1b3751!important; */

div.q-input {
  box-shadow: none!important;
}

.q-slider-handle.dragging .q-slider-label {
  transform: translateX(-50%) translateY(-139%) scale(1)!important;
}

.q-slider-handle.dragging {
  transform: translate3d(-50%, -50%, 0)!important;
}

.modal-buttons.row .q-btn {
    background: rgba(0, 0, 0, 0.05);
}

.q-notifications {
  position: relative;
  z-index: 9999999;
}